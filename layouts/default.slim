doctype html
html
  head
    == render '/header.*'
  body
    .pusher
      #content
        == render '/menu.*'
        == yield

      footer
        == render '/footer.*'

    == render '/sidebar.*'
